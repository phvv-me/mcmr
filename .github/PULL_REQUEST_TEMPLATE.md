## Summary

Describe what this change does and why.

## Checklist

- [ ] Ran `uv run ruff check .` and `uv run ruff format --check .`.
- [ ] Ran `uv run mypy src`, `uv run pyrefly check`, and `uv run ty check --error-on-warning`.
- [ ] Ran `uv run pytest`.
- [ ] Updated `README.md`, `SYSTEM.md`, and `CHANGELOG.md` if user-facing.
